module.exports = {
    testEnvironment: 'node',
    testMatch: ['**/tests/**/*.test.js'],  // Look for tests in the "tests" directory
    testPathIgnorePatterns: ['/node_modules/'], // Ignore node_modules
  };
  